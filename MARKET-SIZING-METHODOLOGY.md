# Global GTM 시장규모 산정 방법론 v3 — Top-Down

## 원칙

- AI 시장·경쟁 사전조사의 TAM·SAM·SOM·교두보 시장은 **Top-Down 방식**으로만 추정한다.
- 창업자의 가격·고객 수·판매역량·자원 입력은 시장규모 계산에 사용하지 않는다.
- GPT-5.6 Luna는 공개자료를 찾아 계산 입력 범위와 대리 가정을 제안하고, 서버가 출처·최신성·계층을 검증한 뒤 모든 산술을 다시 계산한다.
- 공개 근거가 부족하면 숫자를 만들지 않고 `insufficient_evidence`로 표시한다.

## 1. 시장 정의

제품 범위, 목표 국가, 목표 고객, 포함·제외 범위, 채널, 통화, 기준연도와 연간 매출 단위를 먼저 고정한다.

## 2. TAM

최근 3년 이내의 서로 독립적인 공개 시장매출 경로 두 개를 사용한다. 두 경로는 URL·발행기관·도메인이 달라야 한다.

`TAM = 공개 시장매출 경로 2개의 낮음·기준·높음 범위 평균`

## 3. SAM

`SAM = TAM × 목표 지역 × 고객 적합성 × 채널 접근성 × 규제 통과 가능성`

네 필터를 분리하고 각각 공개 출처를 연결한다. 서버는 `SAM ≤ TAM`을 검증한다.

## 4. SOM

`SOM = SAM × 3~5년 획득 가능 점유율`

점유율은 공개 벤치마크에 근거한 1~5% 범위를 사용한다. 회사의 실제 판매·공급 역량을 반영한 예측이 아니라는 점을 가정과 한계에 표시하고, 서버는 `SOM ≤ SAM`을 검증한다.

## 5. 교두보 시장

`교두보 시장 = SAM × 공개자료로 확인한 최초 집중 세그먼트 비중`

최초 세그먼트는 유사 제품 구매, 유사 판매주기, 입소문 가능성을 모두 점검하고 인접시장 확장 경로를 제시한다. 서버는 `교두보 시장 ≤ SAM`을 검증한다.

## 6. 근거와 표시

- 모든 숫자는 낮음·기준·높음 범위로 표현한다.
- 출처에는 URL, 발행기관, 발행일, 확인일을 포함한다.
- 공개 사실과 대리 가정을 구분하고 창업자 입력을 계산 근거로 허용하지 않는다.
- 화면과 보고서에는 `Top-Down · 공개자료 기반 하향식 추정`이라고 표시한다.
- 기존 v1·v2 보고서는 열람할 수 있지만 새 사전조사의 캐시로 재사용하지 않는다.

## 모델 구성

- 시장동향·경쟁사·시장규모 근거·요약: GPT-5.6 Luna
- 계산·시장 계층·최신성·출처 검증: 애플리케이션 서버
