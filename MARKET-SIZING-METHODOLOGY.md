# Global GTM 시장규모 산정 방법론 v2

## 목적과 책임 분리

- GPT-5.6 Sol은 공개 자료 검색, ICP 퍼널 설계, 대리 가정, 시나리오 비교, 계산 입력 범위를 추론한다.
- 서버는 Sol이 반환한 근거 URL의 검색 결과 포함 여부와 발행기관·발행일의 존재·형식·최신성을 검증하고 TAM·SAM·SOM·교두보 시장을 다시 계산한다.
- 창업자 입력이 있으면 독립된 하향식 공개 근거를 유지하고 허용된 상향식 수치 입력에만 병합한다.
- 근거 없는 숫자를 만들지 않는다. 방어 가능한 공개 대리값이 없을 때만 `insufficient_evidence`를 사용한다.

## 1. 시장 정의

계산 전에 제품 범위, 목표 국가, 목표 고객, 포함·제외 범위, 채널, 통화, 기준연도, 연간 매출 단위를 고정한다. 상향식과 하향식 계산은 같은 시장 정의를 사용해야 한다.

## 2. ICP 퍼널과 시나리오

상향식 고객 수는 다음 퍼널로 추정한다.

`시작 인구 × 인구통계 × 고용·직업 × 소득 × 구매행동 × 채널 접근성 = ICP 고객 수`

각 시나리오는 인구통계·고용·소득·구매행동·채널 단계를 정확히 하나씩 포함한다. A와 C는 모든 단계를 활성화하고, B는 A의 제한 단계 정확히 하나만 비활성화한다. 각 단계와 시나리오별 연간 고객당 매출은 낮음·기준·높음 범위와 공개 출처를 가져야 한다. 결과를 가장 크게 바꾸는 의사결정 변수 하나를 식별하고 서버가 세 시나리오의 TAM을 비교한다.

- 시나리오 A: 창업자가 정의한 ICP 유지
- 시나리오 B: 가장 제한적인 필터를 제거한 확장 ICP
- 시나리오 C: 동일 ICP를 더 적합한 대체 국가에 적용

최종 카드는 목표국가를 유지하는 A 또는 B 중 가장 방어 가능한 시나리오를 사용하고, 대체국가 C와 영향 방향은 TAM 가정·민감도 변수에 기록한다.

## 3. TAM

- 상향식: `ICP 고객 수 × 연간 카테고리 고객당 매출`
- 하향식: 최근 3년 이내 서로 독립적인 공개 사실 경로 2개
- 최종 범위: 동일한 시장 정의의 상향식·하향식 범위를 교차검증

두 방식의 기준값 편차가 20% 이하면 강한 정합성, 20% 초과면 낮은 신뢰도로 설명한다. 100%를 넘으면 시장 정의가 충돌한 것으로 보고 산정을 보류한다. 창업자 입력이 없더라도 독립된 하향식 근거 2개가 있으면 낮은 신뢰도의 TAM을 제시할 수 있다.

## 4. SAM

`TAM × 목표 지역 × 고객 적합성 × 채널 접근성 × 규제 통과 가능성`

네 필터는 서로 분리하고 각각 최신 출처를 연결한다. `SAM ≤ TAM`을 서버에서 검증한다.

## 5. SOM

`min(SAM × 3~5년 획득 가능 점유율, 3~5년 판매·공급 역량)`

점유율은 원칙적으로 1~5% 범위의 공개 벤치마크를 사용한다. 창업자 역량 입력이 없으면 보수적인 외부 대리값을 사용하고 실제 회사 판매역량이 반영되지 않았음을 밝힌다. `SOM ≤ SAM`을 서버에서 검증한다.

## 6. 교두보 시장

`직접 접근 가능한 최초 고객군 수 × 연간 고객당 매출`

최초 고객군은 유사 제품을 구매하고, 판매주기가 비슷하며, 서로 입소문이 가능한 응집된 세그먼트여야 한다. 인접시장 확장 경로도 반드시 제시한다. US$5 million은 계획상 경고 기준으로만 사용하며 시장 유효성의 절대 탈락 조건으로 사용하지 않는다.

## 7. 근거와 가정 규칙

- 공개 사실과 대리 가정은 URL, 발행기관, 발행일, 확인일을 포함한다.
- 하향식 TAM 경로 2개는 URL·발행기관·도메인이 서로 독립적이어야 한다.
- 숫자는 모두 낮음·기준·높음 범위로 표현한다.
- 핵심 가정, 근거 공백, 민감도 변수를 카드에 노출한다.
- 보고서에 출처가 없었던 기존 예시의 숫자는 방법론 참고용일 뿐 플랫폼 계산값으로 복사하지 않는다.

## 모델 구성

- 시장동향·경쟁사·요약: GPT-5.6 Luna
- 시장규모 근거 추론: GPT-5.6 Sol, 높은 추론 강도, 최대 8회 웹 검색
- 계산·계층·최신성·출처 검증: 애플리케이션 서버

Claude 연결은 Sol이 구조화된 공개 근거를 안정적으로 생성하지 못한다는 운영 데이터가 확인될 때만 별도 폴백으로 검토한다.
